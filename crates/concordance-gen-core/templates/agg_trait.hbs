/// {{title-case summary.name}} Aggregate Required Trait
pub trait {{trait-name summary.name}}Aggregate {
    
    // Commands
    {{#each summary.inbound_commands as |input|}}
    fn handle_{{method-name input.name}}(&self, input: {{input.name}}, state: Option<{{../traitname}}AggregateState>) -> anyhow::Result<EventList>;
    {{/each}}


    // Events
    {{#each summary.inbound_events as |input|}}
    fn apply_{{method-name input.name}}(&self, input: {{input.name}}, state: Option<{{../traitname}}AggregateState>) -> anyhow::Result<StateAck>;
    {{/each}}    
}